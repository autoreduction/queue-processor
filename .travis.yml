sudo: required
language: python
python:
  - "2.7"

services:
  - mysql

addons:
  apt:
    sources:
      - mysql-5.7-trusty
    packages:
      - mysql-server

dist: trusty

before_install:
  - mysql_upgrade --force
  - mysql < build/database/travis-db-setup.sql

install:
  - python setup.py develop
  - pip install -r requirements.txt
  - ./build/install/icat.sh
  - ./build/install/activemq.sh

script:
  # ================== Test setup ==================== #
  - ./build/database/generate_testing_database.sh
  - mysql < build/database/populate-reduction_viewer-db.sql

  # ================ Assert Test Setup =============== #
  - nosetests build/tests/test_db_generation.py

  # ================ Static Analysis ================= #
  - ./build/tests/test_pylint.sh

  # ================ Nose unit tests ================ #

  # Utils tests
  - nosetests utils/clients/tests
